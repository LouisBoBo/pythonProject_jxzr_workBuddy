---
description: 写码前中后自检清单，减少 BUG 与安全遗漏
alwaysApply: true
---

# 谨慎写码自检（强制）

凡编写或改动业务逻辑、IO、网络、合并/同步、打包，**必须**按阶段自检；不得「能跑就打包」，不得把安全/边界留到用户提醒后再补。

## 阶段清单

| 阶段 | 必须做到 |
|------|----------|
| **动手前** | 先写 3 条（可简短）：Happy path、最可能错的边界、失败时怎么表现 |
| **涉及 IO/网络** | 读路径：`resolve` + `relative_to` 校验在项目根内；写文件：先 `.tmp` 再原子 `replace`；发 HTTP：走 `safe_http` / `assert_http_url_allowed`，禁止手写 localhost 字符串判断 |
| **改已有函数** | 改完至少跑一条真实调用链，或补/跑一条单测；禁止只看 diff |
| **合并/同步逻辑** | 先定规则：保留什么、更新什么、同 key 冲突怎么办（如 path 相同 id 不同）；再写代码 |
| **收工前** | 跑相关单测 + 一条用户路径（如写码 → 同步 → 查数）；通过后再谈 commit / 打包 |

## 常见反例（今日教训）

- 同一天堆多个大功能（同步 + 解析 + hook + 打包）却不跑端到端
- 变量改名/refactor 后未跑调用方（如 `max_chars` vs `cap`）
- 合并只考虑新增/覆盖，未考虑历史 id、字段级合并（`columns`）
- 路径穿越、非原子写入、URL 校验在「打包前自检」才补——应在设计 IO/网络 时默认完成

## 打包前固定顺序

1. IO/网络 安全项（上表）
2. 相关单测
3. 一条端到端用户路径
4. 再 bump 版本 / 打 dmg

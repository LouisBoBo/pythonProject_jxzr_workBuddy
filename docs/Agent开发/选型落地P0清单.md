# 选型落地 · P0 清单（Deep Agents 架构锁定）

> 状态：**P0 已关闭** · 2026-08-24 你确认通过  
> 依据：[`DeepAgents与DeepSeek-Harness技术选型分析.md`](DeepAgents与DeepSeek-Harness技术选型分析.md) §9  
> 约定：P0 已关闭；**P1 未开**（开前先定「提交前是否审码门禁」，见选型分析 / 本文件「P0 完成后」）。P2（Harness 写码 POC）默认不开。  
> Cursor 规则：`.cursor/rules/agent-harness-deep-agents.mdc`（alwaysApply）

---

## P0 目标（两句）

1. **锁定**：主 harness = Deep Agents；写码执行 = Cursor 旁路；DeepSeek Harness 不作主框架。  
2. **巩固**：结构化车道、HITL、缺口透明、资料包、分析 / 运维 Tools 保持为演进主路径（不换壳）。

---

## 检查表

| ID | 项 | 验收标准 | 状态 | 说明 |
|----|----|----------|------|------|
| **P0-LOCK-01** | 选型文档结论锁定 | 选型分析 §10 正式表述不变；本清单可引用 | ✅ 已完成 | [`DeepAgents与DeepSeek-Harness技术选型分析.md`](DeepAgents与DeepSeek-Harness技术选型分析.md) |
| **P0-LOCK-02** | Cursor 规则 alwaysApply | 后续会话默认遵守主 harness / 旁路 / 禁项 | ✅ 已完成 | `.cursor/rules/agent-harness-deep-agents.mdc` |
| **P0-LOCK-03** | 文档索引可发现 | `docs/README.md` Agent 开发区有选型 + 本清单 | ✅ 已完成 | 本文件 + README |
| **P0-BYPASS-01** | 本机写码默认 Cursor | `LOCAL_DEV_AGENT` 默认 `cursor_sdk`；llm 需 `LOCAL_DEV_ALLOW_LLM_FALLBACK=1` | ✅ 已核对 | `apps/local_dev/config.py` `_env_agent`；`scripts/dev.sh` / desktop 默认 |
| **P0-BYPASS-02** | GitHub 写码旁路 | Cloud 走 `apps/cursor_dev/`，不经 Deep Agents 改仓 | ✅ 已核对 | 现有车道 + 文档 |
| **P0-BYPASS-03** | 主环不写仓 | Agent 提示 / Skill：改码确认后走 propose → job，禁止主环裸写宿主机 | ✅ 已核对 | `cursor-dev-chat` Skill + local_dev 沙箱 |
| **P0-LANE-01** | 前端显式车道 | 写码 / 审核 / 贴码发送时带 `page_context.workbuddy_lane` | ✅ 已核对 | `ChatView.vue`：`code_dev` / `code_review` / `paste_code` |
| **P0-LANE-02** | 后端显式优先 | `resolve_workbuddy_lane`：显式 > 标记兼容；非法值 sanitize | ✅ 已核对 | `workbuddy_lanes.py` + 单测（原问题清单 P0-09） |
| **P0-LANE-03** | 车道单测绿 | `test_workbuddy_lanes` / `test_agent_lane_message` 通过 | ✅ 已完成 | 2026-08-24：19 OK；另 `local_dev.test_fs_snapshot` 含默认 cursor_sdk 4 OK |
| **P0-HITL-01** | 写确认基线 | 导入等写工具 Middleware 拦截 → 确认卡 → API 直执 | ✅ 基线已有 | 持续强化记日常演进，不换框架 |
| **P0-GAP-01** | 缺口透明基线 | `explicit_gap` / 页内 caveat / 演示纪律 | ✅ 基线已有 | 原 P0-01 / P0-12 / P1-02 / P1-03 |
| **P0-PROFILE-01** | 资料包主路径 | 换厂靠 `mes_profiles` + OpenAPI → entities，不写死 | ✅ 基线已有 | 配置 / 摸底工具链 |
| **P0-OPS-01** | 分析运维 Tools | 查数 / 图表 / `run_ops_scene` 挂在 Deep Agents Tools | ✅ 基线已有 | 新能力继续加 Tool/Skill |
| **P0-FORBID-01** | 明确不做 | 无 PR/代码引入 DeepSeek Harness 主环或 `dsh` 产品壳 | ✅ 约束生效 | 规则 + 选型 §9 |

图例：✅ 已完成 / 已核对 · 🔲 待跑验证 · 🟡 处理中 · ⬜ 未开始

---

## 本轮验证命令

在仓库根执行（无 LLM、不改生产）：

```bash
cd apps/api && python -m unittest test_workbuddy_lanes test_agent_lane_message -v
cd ../local_dev && python -m unittest test_fs_snapshot -v
```

通过后将 **P0-LANE-03** 标 ✅，并将本节「P0 状态」改为 **已关闭（待你确认）**。

**本轮已跑通（2026-08-24）**：车道 19 测 + `local_dev` 默认 agent 相关 4 测均 OK。请你确认后关闭 P0，再开 P1。

---

## P0 完成后做什么

| 下一档 | 内容 | 何时开 |
|--------|------|--------|
| **P1** | 自动提交 / 部署：确认卡 → API → CI；**默认：审码门禁 + 人确认后提交** | **P1-1 进行中**（本机） |
| **P2** | 可选：DeepSeek Harness **仅**本机写码执行器 POC | 显式点名再开 |

---

## 与「问题与风险点确认清单」的关系

[`问题与风险点确认清单.md`](../总览/问题与风险点确认清单.md) 里的 **P0×12 / P1×10 已全部关闭**（工程债与话术）。  
**本清单是另一条线**：选型落地（架构锁定 + 旁路/车道核对），编号 **P0-LOCK / BYPASS / LANE …**，勿与问题清单 P0-01 混用。

---

## 变更日志

| 日期 | 动作 |
|------|------|
| 2026-08-24 | 建档；LOCK-01～03、BYPASS、LANE-01/02、HITL/GAP/PROFILE/OPS/FORBID 基线核对；待跑 LANE-03 |
| 2026-08-24 | LANE-03 单测绿；**P0 检查项齐 · 待确认关闭** |
| 2026-08-24 | **P0 已关闭**（你确认）；P1 开前讨论「提交前审码门禁」 |

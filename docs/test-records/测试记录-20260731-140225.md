# 测试记录 — 2026-07-31

> **怎么读本文**：先看下面「验收看板」。  
> - **可验收** = 本轮自动化已通过，功能底座可信，可据此签字（或只需抽查 UI）。  
> - **还不能验收** = 本轮没跑到用户真实路径（要登录页/点按钮/跟大模型多轮聊），自动化绿了也不等于产品验收过。

| 项 | 内容 |
|----|------|
| 依据 | [`docs/功能清单与测试用例.md`](../功能清单与测试用例.md) |
| 时间 | 2026-07-31 13:52 ~ 14:05 |
| 环境 | API `8765` + Web `5180` + 沙箱 `8001` 已启动 |
| 详细日志 | [`run-20260731-140225.log`](./run-20260731-140225.log) |

---

## 验收看板（人话版）

### 一、本轮【可验收】——自动化已通过

这些能力的**后台逻辑/接口/冒烟**已绿，可以当作「工程验收通过」。若产品要求「亲眼看见页面」，括号里写了还需你抽看的一眼。

| # | 功能 | 自动化结论 | 你还要不要点一下 |
|---|------|------------|------------------|
| 1 | 服务健康 / 就绪检查 | 通过 | 不必 |
| 2 | 写操作防双确认（多进程抢确认） | 通过 | 日常导入点一次确认即可抽查 |
| 3 | 历史会话用户隔离（别人看不到你的记录） | 通过 | 换账号看一眼更稳 |
| 4 | 嵌入页上下文（单据号等带进对话） | 通过 | 真 iframe 嵌入建议再验一次 |
| 5 | 实体别名 / 错实体守卫（查工单不串实体） | 通过 | 随便问一句工单类可抽查 |
| 6 | 会话记忆底座（停止半截能同步进上下文） | 通过 | 「停止→继续」仍建议手测一遍 |
| 7 | API 健康分析（日志 + 沙箱探活） | 通过 | 对话里问一次「测接口」可抽查 |
| 8 | IDE Bridge 任务闭环（注册/投递/回传） | 通过 | 真 VS Code 配对走一遍更完整 |
| 9 | IDE 敏感文件拒绝读（如 `.env`） | 通过 | 不必 |
| 10 | IDE mock 审核链路 | 通过 | 真工程审核见「还不能」 |
| 11 | 历史保存 / 读取 / 删除（接口） | 通过 | 侧栏点开会话抽查 |
| 12 | 对话附件上传（接口） | 通过 | 聊天框挂个文件抽查 |
| 13 | 流式对话能开流（SSE 接通） | 通过 | 完整回答内容靠 LLM，见下 |

**一句话**：底层设施和安全隔离本轮可以验收；**聊天好不好用、写确认点得顺不顺，还要人测。**

---

### 二、本轮【还不能验收】——必须人工 / 真环境

下面这些**自动化没覆盖或只盖到底层**，发版前请按表自己点，通过后才算产品验收。

| # | 功能 | 为什么还不能自动算过 | 建议你怎么验 |
|---|------|----------------------|--------------|
| A | ERP 登录 / 未登录跳转 | 要真账号 + 浏览器 | 登录进对话；清登录后再打开应回登录页 |
| B | 停止生成 +「继续/来吧」续写 | 要 UI 点停止 + 多轮 LLM | 长回答中途停止 → 发「继续」→ 看是否新气泡且接得上 |
| C | 复制消息去掉「已停止」标记 | 要 UI | 复制停止气泡，粘贴看有无该字样 |
| D | 编辑旧消息重发 | 要 UI | 改一条早先用户消息重发 |
| E | 侧栏历史打开 / 新会话体验 | 接口过了，侧栏手感未测 | 新开 → 再点回旧会话 |
| F | 平台 iframe 嵌入整页 | 只测了身份逻辑 | 按《平台嵌入说明》用带 token 的 URL 打开 |
| G | 文件管理页格式转换 | 未测该页 | 打开文件管理，转一个 Excel→CSV |
| H | 导入写确认：确认 / 取消 / 过期 | 只测了防双写锁 | 走一次导入：确认成功、取消不写、过期拒绝 |
| I | 自然语言查工单 / 运维话术 | 要 LLM + ERP | 问「最近工单」看结果是否靠谱 |
| J | PCB 行业闲聊不误查库 | 要 LLM | 问「什么是沉金」应解释而非查工单 |
| K | 表结构能力总览 / 场景表 / 摸底报告 | 要 LLM | 问「平台能干什么」；详见专项 10 问 |
| L | 贴码分析 +「来吧」给完整修好版 | 要 LLM 多轮 | 贴一段烂代码 → 分析 →「来吧」 |
| M | VS Code 真机配对 + 选工程审核 | Bridge 底层过了，扩展手测未做 | 按《IDE 代码审核开发者接入手册》15 分钟走通 |
| N | 重启 API 后同一会话短句续聊 | 要停服务再聊 | 聊一轮 → 重启 →「把刚才再说一遍」 |
| O | Docker 双机 HA 整机 | 只测了锁/CAS 脚本 | 按《高可用部署说明》起双实例（可选） |

---

### 三、一眼对照（能不能签字）

| 如果你要签的是… | 本轮结论 |
|-----------------|----------|
| 「冒烟 / 工程底座没坏」 | **可以签**（上表「可验收」全绿） |
| 「用户能流畅用完所有聊天与导入功能」 | **还不能签**（先完成「还不能验收」里的 A～N 抽测） |
| 「IDE 本机审核可演示」 | **半签**：后端 Bridge 可；真 VS Code 配对仍要做 M |
| 「嵌入 ERP 可上线」 | **半签**：身份逻辑可；真 iframe 仍要做 F |

期间修过 2 个自动化自身问题（checkpoint 冒烟挂死、历史接口字段写错），**均已复验通过**，不影响上表「可验收」结论。

---

## 附录 A：自动化套件明细（给开发看）

| 套件 | 结果 | 耗时 | 备注 |
|------|------|------|------|
| `make health` | PASS | ~0s | |
| `GET /health/ready` | PASS | ~1s | |
| `make smoke-ha` | PASS | 16s | |
| `make smoke-embed-identity` | PASS | 12s | |
| `make smoke-entity-phrases` | PASS | 7s | |
| `make smoke-checkpoint-context` | PASS | 13s | 曾挂死，已修 |
| `make smoke-api-health` | PASS | 9s | 探活也过 |
| `make smoke-ide-review-m0` | PASS | 7s | |
| `make smoke-ide-bridge-m1` | PASS | 8s | |
| `make smoke-api-contracts` | PASS | ~14s | 曾 422，已修 |

```bash
make health && curl -sS http://127.0.0.1:8765/health/ready
make smoke-ha
IDE_REVIEW_ENABLED=0 make smoke-embed-identity
make smoke-entity-phrases
make smoke-checkpoint-context
make smoke-api-health
make smoke-ide-review-m0
make smoke-ide-bridge-m1
make smoke-api-contracts
```

---

## 附录 B：期间错误（已处理）

1. **checkpoint 冒烟打印 OK 后进程不退出** → 已关闭连接并强制退出，复验通过。  
2. **历史保存契约误传 `id` 导致 422** → 改为 `thread_id`，复验 13/13 通过。  
3. **观察**：mock 审核会报 embed.js「URL 带 token」P1，属规则命中现有设计，不记失败。

---

## 附录 C：用例编号对照（可选）

完整 TC 编号与步骤仍以 [`功能清单与测试用例.md`](../功能清单与测试用例.md) 为准；本文看板不要求你会记 TC 号。

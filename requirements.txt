# Agent + API 共用依赖（约束范围；安装请用 requirements.lock.txt / make install）
# 更新锁：改本文件后执行 make lock-python，并提交 requirements.lock.txt
deepagents>=0.1.0
langchain-openai>=0.3.0
openai>=1.40.0
langgraph-checkpoint-sqlite>=2.0.0
aiosqlite>=0.20.0
pandas>=2.0.0
openpyxl>=3.1.0
fastapi>=0.115.2
uvicorn[standard]>=0.31.1
python-multipart>=0.0.18
python-dotenv>=1.1.0
# Cursor 研发写码旁路（Cloud Agent）；缺省时 API 仍可启动，写码车道 available=false
cursor-sdk>=1.0.24

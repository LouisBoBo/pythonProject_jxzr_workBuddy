# 每日复盘 · 2026-08-26

> 范围：P1-3 部署收尾（探活 / systemd）+ 设置页分类精简 + 部署配置去硬编码与安全加固 + 提交/推送链路修坑  
> 仓库：WorkBuddy `simplified-workbuddy`；业务仓 `LouisBoBo/pythonProject_zr_aicoding`；预发 `175.178.238.31`（ERP **8090/8009**）  
> 承接：[`2026-08-25.md`](2026-08-25.md)、[`人触发部署确认与CIP1-3方案.md`](../../写码与审码/人触发部署确认与CIP1-3方案.md)  
> 冒烟：`test_deploy_prepare`（探活等）；`test_commit_gate` + `test_batch_commit` **31** 条通过；公网 `http://175.178.238.31:8090/` 曾验 200

---

## 1. 今日目标与结果

| 目标 | 结果 |
|------|------|
| 承接昨日：探活 + 预发 systemd（只动 8009） | **P** |
| 设置页按功能分类（商用模型 / 平台 / 写码 / 审码 / 部署） | **P**（只改展现） |
| 部署区精简：GitHub Actions 与本机 SSH 分开、少废话 | **P** |
| 审码车道：VS Code 配对收进审码区；红框只显示状态 | **P** |
| 部署配置不写死，换项目可配 | **P**（sync pairs / build steps / excludes 等） |
| 未提交代码 BUG/安全自检并修 | **P**（部署注入/路径/poll；单测 DATA_DIR 隔离） |
| 提交批：有改动却「无文件」/ 推送误报网络 / 说明过长 | **P**（porcelain、push-only、NFF→rebase） |
| 对话端到端再跑「部署到预发」 | **部分**（探活单测 + 公网；依赖热更新后手工确认卡） |
| 整包 commit / 发版 | **未做** |

---

## 2. 今日做了什么

### 2.1 上午：P1-3 收尾（探活 + systemd）

- `probe_deploy_health`：空 URL 跳过；非法 scheme 拒绝；2xx 通过；非 2xx 重试后失败  
- 确认卡 / poll 透出 `health_ok` / `health_detail`  
- 预发 `zr-aicoding-api` systemd（`127.0.0.1:8009`）；图谱 **80/8000 未动**  
- 方案文档标 P1-3d 探活已落地  

### 2.2 体验与配置澄清

| 现象 | 结论 / 处理 |
|------|-------------|
| 「开启自动化部署」经常变关 | 拨钮未点「保存并热更新」；`DEPLOY_ENABLED` 未落盘；单测曾污染真实 `DATA_DIR` → 已隔离 |
| 部署过程不明显、成功无地址 | 确认卡加强过程态；成功输出访问地址（探活 URL） |
| 生产治理「还没开」 | 产品说明：预发 ≠ 生产；同机多端口可行但今日不做生产 |
| 私钥在哪 | 文件：`~/.ssh/tc_staging_deploy`；设置只存**路径**，私钥不进 git/settings 内容 |

### 2.3 设置页与车道整理

- 顶部分类标签：商用模型 / 平台接入 / 写码车道 / 审码车道 / 自动化部署  
- 自动化部署：GitHub Actions 与本机 SSH **分区**，去掉冗余说明  
- 审码：`IdeBridgePairCard` 收进审码区；顶栏红框只显示配对状态  

### 2.4 部署可配置化 + 安全加固

- 可配：`DEPLOY_SSH_SYNC_PAIRS` / `BUILD_STEPS` / `RSYNC_EXCLUDES` / workflow env 等；空则保留 monorepo 默认  
- 构建步骤：禁 `shell=True`；白名单 + 拒 `-c` / 注入  
- 路径 `resolve` + `relative_to`；sync 拒绝对路径 / `..`  
- `/deploy/poll`：无 `DEPLOY_GITHUB_REPO` 时不接受客户端随意 `repo`（防 Token 滥用）  
- 确认幂等：`threading.Lock`  

### 2.5 下午：提交 / 推送修坑

| 现象 | 根因 | 处理 |
|------|------|------|
| 提交时报 `findings is not defined` | `ChatView` 确认批后未定义 `findings` | 从 `gate.findings` 安全取数组 |
| 推送失败后提示「说明过长」 | 重试误重新 commit，把 git 错误塞进 message | **push-only**；不把 push 错写入说明；截断 |
| 有改动却「没有文件 / 非 git」 | `_run_git` 对 stdout `.strip()` 吃掉 porcelain 首空格 → ` M path` 变 `ackend/...` | 勿 strip 前导空格；脏文件兜底（排除日志/.vite） |
| GitHub 可访问仍推送失败 | `fetch first` **非网络**；远程 `hebo` 超前 | 分类错误文案；push 遇 NFF 自动 `fetch+rebase` 再推 |

---

## 3. 写码自检（必做）

> 对照 [`.cursor/rules/cautious-coding-checklist.mdc`](../../../.cursor/rules/cautious-coding-checklist.mdc)

| 阶段 | 今日是否做到 | 备注 |
|------|--------------|------|
| 动手前：Happy path / 边界 / 失败表现 | ☑ | NFF→rebase；冲突 abort；探活失败标 job |
| IO/网络：路径 under_root、tmp+replace、safe_http | ☑ | 部署路径/构建注入/poll repo 已收紧 |
| 改已有函数：跑过调用链或单测 | ☑ | commit/batch 31；deploy 探活相关 |
| 合并/同步：冲突规则已事先定好 | ☑ | 车道互斥；部署与提交批隔离 |
| 收工前：单测 + 一条用户路径 | 部分 | 单测绿；推送 rebase / 部署确认卡依赖重启后手工 |
| 打包前 | ☐ | **未打包** |

### 已修（BUG / 安全）

| 现象 | 处理 |
|------|------|
| 构建/同步命令注入、路径穿越 | 白名单、禁 shell、relative_to |
| poll 客户端 repo 滥用 | 未配置仓时拒绝客户端 repo |
| confirm 并发重入 | Lock |
| 单测关 `DEPLOY_ENABLED` 污染本机设置 | 临时 DATA_DIR |
| `findings` 未定义 | 安全默认 `[]` |
| 重试污染中文说明 / 误重新 commit | push-only + 截断 |
| porcelain 路径解析坏 | 保留前导空格 + dirty 兜底 |
| NFF 误报「修网络」 | 错误分类 + 自动 rebase |

### 仍存边界 / 技术债

1. **大改仍未 commit / 未推送 / 未发版**（含 08-25 部署旁路 + 今日全量）。  
2. rebase 遇真实冲突或脏工作区仍需本机手动处理。  
3. Actions 对国内机仍不可用；主路径继续 `local_ssh`。  
4. `DEPLOY_ENABLED` 仍依赖「保存并热更新」；易被误解为拨钮即生效。  
5. 端到端「提交→推送成功 / 部署确认卡探活」建议重启 API 后再点一次确认。

---

## 4. 关键产物

| 区域 | 路径 |
|------|------|
| 部署执行链 | `deploy_*.py`、`test_deploy_prepare.py` |
| 提交 / 推送 | `git_commit.py`、`batch_commit.py`、`LocalDevCommitGateCard.vue` |
| 设置 / 确认卡 | `SettingsView.vue`、`LocalDevDeployGateCard.vue`、`IdeBridgePairCard.vue` |
| 方案 / 示例 | `人触发部署确认与CIP1-3方案.md`、`示例-deploy-staging.yml` |
| 本复盘 | `docs/复盘/每日复盘总结/2026-08-26.md` |

---

## 5. 建议明日关注

1. **整理并 commit** 部署 + 设置分类 + 提交推送修坑（说明写清 why）。  
2. 重启 API 后回归：提交批「重试推送」（含远程超前）+「部署到预发」确认卡探活。  
3. 可选：设置页拨钮旁强化「须保存」提示，减少 `DEPLOY_ENABLED` 反复关误解。  
4. 生产环境仍按规划暂缓；预发稳定后再谈端口隔离生产。

---

## 6. 一句话结论

预发旁路与探活已收口，设置与部署配置可换项目使用；提交推送几处真实坑（路径解析、说明污染、非快进）已修并通过单测——**整包尚未提交**，明日优先落地 commit 与一次手工端到端。

# 每日复盘 · 2026-08-19

> 范围：上午 P0 查数验收；下午本机写码切 Cursor SDK + 安全自检；晚间 MES 资料包自动同步、查数别名优化、0.1.5/0.1.6 打包与二次自检  
> 仓库：WorkBuddy `simplified-workbuddy`；联调资料包「江西中软MES系统」；MES/ERP 样例 API `8009`  
> 承接：[`2026-08-18.md`](2026-08-18.md)  
> 冒烟：`scripts/smoke_mes_acceptance_0819.py`（查数 / 闸门）+ Cursor Local 真改文件冒烟 + mes_profile 相关单测 18 项

---

## 1. 今日目标与结果

| 目标 | 结果 |
|------|------|
| 换平台负向 / TC-Q-03 / 04 / 05 / D 闸门 | **P**（见上午冒烟） |
| 过程区中文表 + 当日完工诚实降级 | **P** |
| 查清 DeepSeek 18–19 日 token 暴涨 | **已定位**：`deepseek-v4-pro` + Peak 计价 + 本机写码工具环反复上传大缓存 |
| 本机路径写码走 Cursor，不走 DeepSeek | **已落地**：默认 `LOCAL_DEV_AGENT=cursor_sdk`；GitHub 仍走 Cloud |
| Cursor Local 端到端 | **P**：job `ldj-3cc309a5bcbf41a7`，约 29s，`runtime=cursor_local` |
| 写码后 API/表结构自动同步资料包 | **P**：预览就绪 hook → 本机 OpenAPI 合并写 entities + 模型增量写 schema |
| 查库存 / 「库存列表」误匹配看板 | **P**：路径别名 + 分词打分；「列表」优先明细接口 |
| Git 代码审核崩溃 | **已修**（0.1.5）：`_trim_file_contents` 误用 `max_chars=None` 与整数比较 |
| 桌面打包 0.1.6 | **P**：`desktop/release/ZR WorkBuddy-0.1.6.dmg`（约 209MB） |
| 晚间改动二次自检（BUG / 安全） | **已修 6 项**（见 §3）；单测 18 项全绿 |
| 写码规范沉淀 | **P**：`.cursor/rules/cautious-coding-checklist.mdc` + 复盘 `_template.md` |
| 系统配置文案 MES → MES / ERP | **P**（标签由 API 下发；前端加兜底；须重启 API 才生效） |
| 清理多余后台 | **P**：只留 8765 / 5180 / 19801 + ERP 8009 |

---

## 2. 今日做了什么

### 2.1 上午：查数验收与体验

- 清端口后 `dev.sh`；冒烟覆盖换平台负向、TC-Q-03（嵌套 path）、TC-Q-04（无接口账号挡住）、TC-Q-05（摸底≠查数）、当日完工 caveat、D 闸门补列回填。
- 过程区：`display_rows` / `markdown_table` 用中文列；caveats 进预览。
- `query-mes-data` Skill：禁止把缺日期筛参说成「今天完工 N 条」。
- 文档：摸底 10 句、四块能力方案缺口表、查数用例 08-19 行。

### 2.2 下午：费用根因

控制台 18–19 日约 **548 万 token**，高峰 14:00–16:00，以 cache-hit 输入为主。

| 判断 | 说明 |
|------|------|
| 不是查数把库扫爆 | B 查数走 MES HTTP，不把表结构当上下文反复烧 |
| 主因是本机写码 | `target=local` 当时走 DeepSeek 工具环，每步把沙箱文件摘要塞回模型 |
| 模型与计价叠加 | `deepseek-v4-pro` + 8/16 涨价 + Peak；Flash 够 MES，写码稳定性差一些 |
| 两条写码车道 | 本机路径 → 当时 DeepSeek；GitHub → Cursor Cloud。用户要「写码都走 Cursor」 |

处理：本机默认改 Cursor SDK Local Agent；应急仍可 `LOCAL_DEV_AGENT=llm`。

### 2.3 下午：本机路径 + Cursor SDK

拷贝到 `data/local_dev/sandboxes/<job>/` → `Agent.create(..., local=LocalAgentOptions(cwd=sandbox, sandbox_options=enabled, setting_sources=[]))` → 快照 diff → import 闸门 → 同步目标目录。

| 件 | 路径 |
|----|------|
| Local Agent | `apps/local_dev/cursor_local_agent.py` |
| 快照 / diff | `apps/local_dev/fs_snapshot.py` |
| 接线 | `service.py`、`config.py`、`jobs.py`、`routes/local_dev.py` |
| 文案 / Skill | `cursor-dev-chat`、`ChatView.vue`、选仓卡 |

冒烟：沙箱内写出 `SMOKE_CURSOR_LOCAL.txt`（`workbuddy-cursor-local-ok`）。

### 2.4 下午：联调与产品文案

- **`describe_entity`**：list 项偶发字符串，对 `str` 调 `.keys()` 崩溃，界面误报「审核过程异常」→ 已容错。
- **ERP 登不上**：不是 WorkBuddy 鉴权；`:8009` 挂掉 + `seed_equipment_repair_data` `IndexError`（仓外样例工程）→ 修 seed 后重启，`admin` / `admin123` / `江西中软` 可登。
- **系统配置**：分组与账号/密码/企业编码改为「MES / ERP …」；前端 `fieldDisplayLabel` 兜底，避免 API 未重启仍显示旧标签。
- **后台清理**：杀掉十余个 `tail -f` / 旧 `dev.sh` / `:8001` 旧沙箱 / 残留 `cursor-sdk-bridge`；保留一套开发栈。

### 2.5 晚间：资料包自动同步 + 查数别名 + 打包

**背景**：用户通过 WorkBuddy 写码新增库存 API 后，查「库存列表」仍失败——资料包 `entities.json` 未随代码变更更新，且别名把请求打到看板汇总接口。

| 能力 | 说明 |
|------|------|
| 写码后自动同步 | `mes_profile_sync.py`：检测 synced 文件是否动 API / 模型 → 从本机 MES 拉 OpenAPI **合并**写 `entities.json`（不删旧实体）→ SQLAlchemy 解析增量写 `schema.md` |
| 开关 | `MES_PROFILE_AUTO_SYNC=0` 可关；默认开 |
| Agent 工具 | `refresh_mes_profile_from_runtime`：运行中 MES 手动/Agent 刷新目录 |
| 别名优化 | `openapi_to_entities` 路径推导别名；`entity_catalog.resolve_entity_id` 分词打分，「列表」降看板权重 |
| 联调补刷 | 桌面资料包 `entities.json` 29 → 34 实体，含 `warehouse-inventory-stock` |
| 打包 | **0.1.5** 修 Git 审核崩溃；**0.1.6** 含自动同步与别名优化 |

**根因教训**：功能堆在同一天（同步 + 解析 + hook + 打包），边界与安全项靠打包前才扫出来；已沉淀写码 checklist 规则（§3）。

---

## 3. 写码自检（必做）

> 对照 [`.cursor/rules/cautious-coding-checklist.mdc`](../../../.cursor/rules/cautious-coding-checklist.mdc)

| 阶段 | 今日是否做到 | 备注 |
|------|--------------|------|
| 动手前：Happy path / 边界 / 失败表现 | ☑ 晚间补做 | 合并冲突规则（path 同 id 不同）首版未事先定 |
| IO/网络：路径 under_root、tmp+replace、safe_http | ☑ 晚间补修 | 0.1.6 dmg **早于** 此三项修复 |
| 改已有函数：跑过调用链或单测 | ☑ | ide_review、entity 解析有单测 |
| 合并/同步：冲突规则已事先定好 | ☐ → ☑ 已补 | `merge_entities` 按 path 保留旧 id |
| 收工前：单测 + 一条用户路径 | ☑ | 18 项单测 OK；写码→同步→查库存路径 evening 验证 |
| 打包前：上述通过后才开始 bump/dmg | ☐ | **0.1.6 应先 repack / bump 0.1.7** |

### 已修（BUG / 安全）

| 现象 | 处理 |
|------|------|
| 冒烟先跑空目录会污染表结构缓存 | 先真实资料包，再负向 |
| 过程区查数预览英文 | `_result_detail` 优先中文 `display_rows` |
| DeepSeek 写码烧 token | 本机默认 Cursor Local，不再默认工具环 |
| 符号链接可把宿主机文件拷进沙箱 / 同步写出 | 拷贝跳过 symlink；同步只写普通文件；FS 工具拒绝链接 |
| Cursor follow-up 丢隔离 | `Agent.resume` 带上 `local=` + sandbox |
| import 修复失败仍同步 | 循环后统一 leftover 检查，不通过则中止 |
| Git 审核 `_trim_file_contents` 崩溃 | `max_chars=None` 改比较 `cap`（0.1.5） |
| `merge_entities` 同 path 不同 id 重复实体 | 按 path 匹配保留旧 id |
| 实体合并未带 `columns` | 合并时补充字段 |
| `collect_tables_from_files` 路径穿越 | `_is_under_root` 校验 |
| localhost URL 仅字符串匹配 | `assert_http_url_allowed` + loopback IP/DNS |
| `entities.json` 非原子写入 | `.tmp` + `replace` |
| 改了 settings 标签页面不变 | API 无 reload；杀残留 PID 后重启；前端再兜底 |
| 多套 `dev.sh` 占端口 + ∞ 终端 | 清 tail 与旧包装进程 |

### 仍存边界 / 技术债

1. 「当日完工」无日期筛参只能降级；接口侧无法凭空补参数。
2. Cursor Local ≠ Docker：靠 SDK sandbox + 提示词 + 同步闸门；恶意 Agent 越界仍可能尝试，落盘多半会被挡。
3. D↔B「改完自动跑查数」未做；闸门只动工程内 SQLite。
4. 控制台仍可能有 GitHub Cloud 写码费用（与本机 DeepSeek 分开记账）。
5. **0.1.6 安装包不含晚间安全修复**，需 repack 或 0.1.7。
6. Settings UI 尚无「刷新接口目录」按钮（后端 `upload-entities` 已有）。
7. 已 commit 并推送 `hebo` 分支；仍排除 `.env`、`data/settings.json`、资料包密钥、`data/local_dev/`。

---

## 4. 关键产物

| 区域 | 路径 |
|------|------|
| 过程区中文表 | `apps/api/agent_wrapper.py`、`ProcessPanel.vue` |
| 查数实体容错 | `apps/agent/tools/platform_api.py` |
| 本机 Cursor | `cursor_local_agent.py`、`fs_snapshot.py`、`service.py` |
| 沙箱安全 | `sandbox.py`、`tools_fs.py`、`test_sandbox_security.py` |
| 资料包持久化 / 刷新 | `mes_profile_persist.py`、`mes_profile_refresh.py` |
| 写码后自动同步 | `local_dev/mes_profile_sync.py`、`service.py` hook |
| 查数别名 / 实体解析 | `entity_catalog.py`、`openapi_to_entities.py` |
| Git 审核修复 | `tools/ide_review/tool.py` |
| 写码规范 | `.cursor/rules/cautious-coding-checklist.mdc`、复盘 `_template.md` |
| 单测 | `test_mes_profile_persist.py`、`test_mes_profile_sync.py`、`test_entity_resolve_inventory.py` |
| 安装包 | `desktop/release/ZR WorkBuddy-0.1.6.dmg` |
| 配置文案 | `apps/api/routes/settings.py`、`SettingsView.vue` |
| 冒烟 | `scripts/smoke_mes_acceptance_0819.py` |
| 摸底 / 查数 / 方案 | `docs/MES业务/`、`docs/总览/MES懂行助手四块能力方案.md` |

---

## 5. 建议明日关注

1. **repack 0.1.6 或 bump 0.1.7**：纳入路径校验、safe_http、原子写入后再发 dmg。
2. ~~白名单提交并推送~~（本次已完成；后续仍排除密钥与 `data/local_dev`）。
3. 端到端再走一遍：本机写码改 API → 自动同步 → 对话查「库存列表」。
4. UI 再走一遍：本机目录写码应显示 Cursor Local；过程区不再出现 DeepSeek 工具环。
5. 对话抽检摸底话术 #3～#7、#10（「今天有多少工单」应走 B）。
6. 费用：确认本机写码后 DeepSeek 输入量明显下降；Cloud 写码单独看 Cursor 账单。
7. 仍不做：只读 SQL、字段 ACL、Windows 重打包（除非有人催）。

---

## 6. 一句话结论

上午查数 P0 跑绿；下午本机写码切 Cursor SDK 并补沙箱安全；晚间落地资料包自动同步与查数别名、打出 0.1.6，二次自检又修 6 项 BUG/安全并沉淀写码 checklist——**0.1.6 dmg 需重打才含晚间修复**，代码已 commit 推送。

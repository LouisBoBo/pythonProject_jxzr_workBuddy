# 周报 · 2026-08-10～08-14

> 周期：2026-08-10（周一）～ 2026-08-14（周五）  
> 主题：写码从「GitHub Cloud 可用」推进到「本机目录默认 + 沙箱预览」，并打通 **ZR WorkBuddy** 桌面双交付  
> 仓库：WorkBuddy `simplified-workbuddy`；本机 ERP 演示仓桌面路径 `pythonProject_zr_aicoding`  
> 依据：[`docs/复盘/每日复盘总结/`](../每日复盘总结/)（08-10 / 08-11）及本周提交 `d5b6cfd` → `1e6f339`

---

## 1. 本周一句话

把写码主线从「聊天里改白名单 GitHub」补齐为**本机目录默认写码**（沙箱 → 同步 → 自动预览），修通会话切换与任务可恢复性；同期落地 **Electron 桌面安装包**（产品名 **ZR WorkBuddy**），并加固本机 import 闸门、Git 审码与 HTTPS 镜像安全。截图写码改为质量优先与意图分流，像素级 1:1 仍未闭环。

---

## 2. 本周完成事项（按主题，不夸大）

### 2.1 截图意图与写码质量（08-10，`d5b6cfd`）

| 事项 | 实际状态 |
|------|----------|
| 看图失败 | **已修**：`vision_describe` 缺提示词常量导致 `NameError` |
| 质量 vs 速度 | 认清「加速档」会用占位图交差；改为 **1:1 质量优先**，禁 Unsplash/假 Logo |
| 截图意图分流 | `full_match` / `guided_edit` / `redesign` / 答疑；前端意图卡同步拆分 |
| 产品设计 Skill | 新增 `ui-product-design`；重做优先于盲目整页复刻 |
| 原图 / 上传路径 | `cloud_images`、`upload_paths`；路径安全单测（穿越/symlink）通过 |
| ERP OpenAPI 中文 | 样例仓侧补中文 tags/summary；本仓固化 Cursor Rule「后端 API 必须中文注释」 |
| 配对码复制反馈 | Toast + 「已复制」按钮态 |

### 2.2 写码可恢复性与登录回落（08-11，`ab25baf`）

| 事项 | 实际状态 |
|------|----------|
| 断流 / 云端失败 | 软挂接、对账；取消后不可再写成成功 |
| 确认卡 | 可强制结束 / 重试 |
| 本地 ERP 不可达 | 主地址连不上时**自动回落探活沙箱**，避免登录 Connection refused 卡死 |

### 2.3 本机目录写码主线（08-11～08-12，`a1106f7`）

| 事项 | 实际状态 |
|------|----------|
| 默认目标 | 确认卡默认「本地目录」；GitHub 保留为第二入口 |
| 流程 | 澄清 → 确认 → **沙箱改码** → 闸门 → **sync** → **ensure 预览 URL** |
| 沙箱安全 | 拦 `.git`/敏感路径；拒 home、Desktop/Documents/Downloads 整根；无 `user_id` 读 SSE 直接 403 |
| 同步后预览 | `ensure_dev_preview`：健康复用 / 不健康重启；确认卡可点开预览 |
| 任务状态机 | `try_claim_job` 防双 SSE；取消可打断同步/预览；同步 tmp→replace |
| 会话 UX | 修「新对话/历史无响应」；按 thread 强制重挂载；进度卡不再双份刷 |
| 系统配置页 | `settings.json` 热更新（对话模型 + 写码车道 Key），保存即生效 |
| 方案文档 | [`本机目录写码与沙箱隔离方案.md`](../../写码与审码/本机目录写码与沙箱隔离方案.md) |

**边界（写进验收预期）：** 目录沙箱 ≠ 容器隔离；确认目录内业务源码仍可被改——靠确认卡约束。

### 2.4 桌面双交付与安全加固（08-14，`1e6f339`）

| 事项 | 实际状态 |
|------|----------|
| Electron 壳 | `desktop/` + `scripts/package-desktop.sh` / `run-desktop-dev.sh` |
| 运行时 | 内嵌 Python（python-build-standalone + venv），非 PyInstaller；静态托管 web dist |
| 产品名 | 对外统一 **ZR WorkBuddy**（`build.productName` / 窗口标题规则） |
| 用户数据 | `DATA_DIR` 落 Electron `userData`；Key 不进安装包 |
| 文档 | [`桌面端与网页双交付方案.md`](../../桌面与部署/桌面端与网页双交付方案.md)、[`桌面端安装与配置说明.md`](../../桌面与部署/桌面端安装与配置说明.md) |
| 本机写码闸门 | 相对 import 检查（`import_check`）同步前拦截坏导入 |
| Git 审码 | 工具默认挂载；修末批压制终稿；可配置 HTTPS 镜像并校验防 SSRF |

样例/ERP 侧同周穿插：维修路由被动态段吞掉 → 迁到 `/api/equipment-repairs`；预览「假复用」改为须过 HTTP 健康检查。

---

## 3. 明确未完成 / 已知局限

1. **像素级 1:1 复刻未闭环**：质量优先已落地，登录页等真资源对齐仍需续轮。  
2. **桌面包**：Mac 可打 dmg（本机为 x86_64）；Windows 需在 Windows 主机打；签名/公证未做。  
3. **上传路径形态**：前端仍可能带回绝对 `path`；建议只传 `saved_name`（上周遗留）。  
4. **`/api/download` 鉴权**仍待收口。  
5. **本机写码不上服务器多用户**：选目录弹窗在跑 API 的机器上；远程部署需另评隔离模型。  
6. **08-12～08-14 无单独日复盘文档**：事实以提交说明与方案文档为准；本周报已补齐汇总。

---

## 4. 本周踩过的坑（如实）

- 「2 分钟完成 1:1」≠ 链路坏了，是加速档 + 简单页换了观感。  
- 看图失败先查日志，根因是常量未定义，不是模型抽风。  
- 会话侧栏失效：未清写码轮询 + HMR 损坏 vnode → 按 thread 强制重挂载。  
- 预览「端口在听就算健康」→ 后端坏 import 仍被复用 → 健康必须 HTTP 2xx。  
- ERP 登录死磕 `:8000` → 本地无 ERP 时整站进不去 → 回落探活沙箱。  
- 维修 API 被 `/equipment/{id}` 吃掉 → 独立前缀。

---

## 5. 关键提交与文档

| 提交 | 说明 |
|------|------|
| `d5b6cfd`（08-10） | 写码质量/意图、vision、过程区；上周周报与日复盘入库 |
| `ab25baf`（08-11） | 写码可恢复性；登录回落沙箱 |
| `a1106f7`（08-12） | 本机目录写码主线 + 配置页 + 会话/状态机 |
| `1e6f339`（08-14） | 桌面双交付 + import 闸门 + 审码/镜像安全 |

| 文档 | 说明 |
|------|------|
| `docs/写码与审码/本机目录写码与沙箱隔离方案.md` | 本机写码默认路径 |
| `docs/桌面与部署/桌面端与网页双交付方案.md` | Electron + 网页互不影响 |
| `docs/桌面与部署/桌面端安装与配置说明.md` | 安装与配 Key |
| `docs/复盘/每日复盘总结/2026-08-10.md`、`08-11.md` | 日复盘 |
| `docs/复盘/周报/2026-08-10_08-14.md` | 本文 |

---

## 6. 下周建议（可执行、可验收）

1. 桌面：用本机 dmg 做一次干净安装冒烟（配 Key → 登录 → 对话 → 本机写码/预览）；需要 Windows 包则在 Windows 主机全量重打。  
2. 收口安全遗留：`file_paths` 只传 `saved_name`；评估 `/api/download` 鉴权。  
3. 本机写码：长任务 + 频繁热更新后侧栏稳定性；预览依赖超时/端口冲突的用户可读提示。  
4. 截图写码：选 1～2 个真实页做「质量优先」续轮，对照截图记差距清单（勿再报「已 1:1」）。  
5. 若推进 MES「真数据」：先打通可达的 `PLATFORM_BASE_URL`（或只读 API），再谈分析/运维话术（见总览四块能力方案，属后续产品线）。

---

## 7. 结论（给同步用）

本周主线是 **写码主战场切到本机目录**（沙箱、同步、预览、会话与取消竞态），并把 **ZR WorkBuddy 桌面安装包**从方案落到可打包；GitHub Cloud 写码仍保留为第二入口。质量上纠正了「加速换假像」的方向，看图与意图分流更稳，但界面像素级复刻与部分安全收口仍未完。不宜对外表述为「已容器级隔离」或「桌面已签名可正式分发」。
